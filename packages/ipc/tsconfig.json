{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Sin "DOM": este paquete corre también fuera del navegador. Lo que
    // necesita del entorno (temporizadores, señales) se inyecta desde env.ts.
    "composite": true,
    "lib": ["ES2022"],
    "types": [],
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src/**/*"]
}

{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // DOM solo por AbortSignal, que es el tipo que atraviesa la fachada.
    // No se toca document ni window: este paquete corre dentro de un Worker.
    "composite": true,
    "lib": ["ES2022", "DOM"],
    "types": [],
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src/**/*"]
}

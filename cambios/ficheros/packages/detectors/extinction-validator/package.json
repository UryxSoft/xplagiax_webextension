{
  "name": "@xpx/extinction-validator",
  "version": "0.1.0",
  "description": "Etapa de validación heurística basada en el método de v81d/extinction. Paquete AISLADO bajo GPL-3.0: no lo importan ni el kernel ni el SDK.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "NOTICE.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "//typecheck": "--build, no -p: este paquete referencia al kernel, y sin construir sus declaraciones primero @xpx/kernel no resuelve. --noEmit es incompatible con proyectos referenciados que sí emiten.",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@xpx/kernel": "workspace:*"
  }
}

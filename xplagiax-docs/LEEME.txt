CAMBIOS PARA UryxSoft/xplagiax_webextension
Base: 9588f53   Rama destino: claude/ai-content-detection-platform-k3pa7d

IMPORTANTE: no descomprimas este .tar.gz dentro del repositorio.
La vez anterior el contenido acabo commiteado bajo pkg/ y con el propio
.tar.gz dentro del repo. Este paquete corrige exactamente eso.

QUE HACE ESTE CAMBIO
  1. Mueve pkg/docs -> docs/ , pkg/README.md -> README.md , pkg/.gitignore -> .gitignore
  2. Borra pkg/ y cambios_extension.tar.gz del repositorio
  3. Anade docs/adr/ADR-010-extinction-gpl.md (Extinction es GPL-3.0)
  4. Actualiza 8 documentos: Extinction real, jurisdiccion canadiense
  No toca chrome_extension/, firefox_extension/, edge_extesion/,
  opera_extension/ ni Safari_extension/.

FORMA RECOMENDADA
  tar xzf cambios_extension.tar.gz -C /tmp
  bash /tmp/xplagiax-docs/aplicar.sh /ruta/a/xplagiax_webextension

  El script verifica el estado, crea la rama, aplica el parche y comprueba
  el resultado. No hace push: eso lo decides tu.

FORMA MANUAL
  cd /ruta/a/xplagiax_webextension
  git fetch origin main
  git checkout -B claude/ai-content-detection-platform-k3pa7d 9588f53
  git am --3way < /tmp/xplagiax-docs/cambios.patch
  git push -u origin claude/ai-content-detection-platform-k3pa7d

SI EL PARCHE FALLA
  arbol-final/ contiene README.md, .gitignore y docs/ ya en su sitio.
  Copialos a la raiz del repo y borra a mano pkg/ y cambios_extension.tar.gz.

POR QUE NO SE SUBIO DESDE LA SESION
  git push       -> 403 (el proxy de la sesion bloquea escritura)
  API de GitHub  -> 403 "Resource not accessible by integration"
  La sesion tiene lectura sobre el repo, no escritura.
